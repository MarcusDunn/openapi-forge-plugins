[input]
spec = "spec.json"

[[transformers]]
wasm = "../../target/wasm32-wasip2/release/transformer_filter_operations.wasm"
config = { keep = ["listPets", "createPet", "showPetById", "findPetsByTag", "replacePet", "getPetProblem", "downloadPetPhoto"] }

[generator]
wasm = "../../target/wasm32-wasip2/release/generator_rust_tower.wasm"

[output]
dir = "out"
